// Placeholder removed: the old generic list-of-entities UI no longer applies
// now that `BuildingsState` models a single building doc rather than a list
// (see `buildings_plan.md` for the real UI task list, still pending design).
